import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.test2',
  appName: 'test2',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
